import { HoverOnFocusDirective } from './hover-on-focus.directive';

describe('HoverOnFocusDirective', () => {
  it('should create an instance', () => {
    // @ts-ignore
    const directive = new HoverOnFocusDirective();
    expect(directive).toBeTruthy();
  });
});
